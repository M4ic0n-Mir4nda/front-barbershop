import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    breakpoints: {
      values: {
        xs: 0,
        sm: 360,
        md: 760,
        lg: 1000,
        xl: 1536,
      },
    },
  });
export default theme;



